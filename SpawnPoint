using UnityEngine;

[RequireComponent(typeof(Unit))]

public class SpawnPoint : MonoBehaviour
{
    [SerializeField] private Unit _anemy;

    public Unit Anemy => _anemy;
    public Transform Positinion => transform;
}  
